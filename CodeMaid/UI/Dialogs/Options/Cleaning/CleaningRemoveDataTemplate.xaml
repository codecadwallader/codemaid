<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SteveCadwallader.CodeMaid.UI.Dialogs.Options.Cleaning"
        xmlns:cnv="clr-namespace:SteveCadwallader.CodeMaid.UI.Converters"
        xmlns:properties="clr-namespace:SteveCadwallader.CodeMaid.Properties"
        xmlns:enums="clr-namespace:SteveCadwallader.CodeMaid.UI.Enumerations">
    <DataTemplate DataType="{x:Type local:CleaningRemoveViewModel}">
        <StackPanel>
            <GroupBox Header="{x:Static properties:Resources.Whitespace}">
                <StackPanel>
                    <TextBlock Text="{x:Static properties:Resources.RemoveBlankLines}" Margin="5,5,5,0" />
                    <WrapPanel Margin="35,0,5,10">
                        <ToggleButton Content="{x:Static properties:Resources.AtTopOfFile}" IsChecked="{Binding RemoveBlankLinesAtTop}" />
                        <ToggleButton Content="{x:Static properties:Resources.AfterAttributes}" IsChecked="{Binding RemoveBlankLinesAfterAttributes}" />
                        <ToggleButton Content="{x:Static properties:Resources.AfterOpeningBraces}" IsChecked="{Binding RemoveBlankLinesAfterOpeningBraces}" />
                        <ToggleButton Content="{x:Static properties:Resources.BeforeClosingBraces}" IsChecked="{Binding RemoveBlankLinesBeforeClosingBraces}" />
                        <ToggleButton Content="{x:Static properties:Resources.BeforeClosingTags}" IsChecked="{Binding RemoveBlankLinesBeforeClosingTags}" />
                        <ToggleButton Content="{x:Static properties:Resources.BetweenChainedStatements}" IsChecked="{Binding RemoveBlankLinesBetweenChainedStatements}" />
                        <ToggleButton Content="{x:Static properties:Resources.AtBottomOfFile}" IsChecked="{Binding RemoveBlankLinesAtBottom}" />
                    </WrapPanel>

                    <TextBlock Text="{x:Static properties:Resources.RemoveBlankSpaces}" Margin="5,5,5,0" />
                    <WrapPanel Margin="35,0,5,20">
                        <ToggleButton Content="{x:Static properties:Resources.BeforeClosingAngleBrackets}" IsChecked="{Binding RemoveBlankSpacesBeforeClosingAngleBrackets}" />
                    </WrapPanel>

                    <CheckBox Content="{x:Static properties:Resources.RemoveEndOfFileTrailingNewLine}" IsChecked="{Binding RemoveEndOfFileTrailingNewLine}" />
                    <CheckBox Content="{x:Static properties:Resources.RemoveEndOfLineWhitespace}" IsChecked="{Binding RemoveEndOfLineWhitespace}" />
                    <CheckBox Content="{x:Static properties:Resources.RemoveMultipleConsecutiveBlankLines}" IsChecked="{Binding RemoveMultipleConsecutiveBlankLines}" />
                </StackPanel>
            </GroupBox>
            <GroupBox Header="{x:Static properties:Resources.Regions}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{x:Static properties:Resources.RemoveRegions}" VerticalAlignment="Center" />
                    <RadioButton Content="none" IsChecked="{Binding RemoveRegions, Converter={x:Static cnv:EnumToBooleanConverter.Default}, ConverterParameter={x:Static enums:NoneEmptyAll.None}}" />
                    <RadioButton Content="empty" IsChecked="{Binding RemoveRegions, Converter={x:Static cnv:EnumToBooleanConverter.Default}, ConverterParameter={x:Static enums:NoneEmptyAll.Empty}}" />
                    <RadioButton Content="all" IsChecked="{Binding RemoveRegions, Converter={x:Static cnv:EnumToBooleanConverter.Default}, ConverterParameter={x:Static enums:NoneEmptyAll.All}}" />
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>