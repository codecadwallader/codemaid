<Window x:Class="SteveCadwallader.CodeMaid.Options.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:SteveCadwallader.CodeMaid.Options" mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:OptionsViewModel, IsDesignTimeCreatable=True}"
        Title="CodeMaid Options" MinHeight="480" MinWidth="640" Height="600" Width="800"
        ShowInTaskbar="False" WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SteveCadwallader.CodeMaid;component/Resources/CodeMaidColorResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TreeView Margin="5">
            <TreeViewItem Header="Cleaning" IsExpanded="True">
                <TreeViewItem Header="Automatic" />
                <TreeViewItem Header="File Types" />
                <TreeViewItem Header="Stuff" />
            </TreeViewItem>
            <TreeViewItem Header="Digging (Spade)"  />
            <TreeViewItem Header="Progressing"  />
            <TreeViewItem Header="Reorganizing"  />
            <TreeViewItem Header="Switching"  />
        </TreeView>

        <Border Grid.Row="1" Grid.ColumnSpan="2" Background="{StaticResource FCodeMaidGray}" />

        <Button Grid.Row="1" Content="Reset to defaults" Command="{Binding ResetToDefaultsCommand}" Margin="5" />

        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Command="{Binding SaveCommand}" Margin="5" IsDefault="True" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="5" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>