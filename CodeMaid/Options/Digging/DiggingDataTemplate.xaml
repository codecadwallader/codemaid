<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SteveCadwallader.CodeMaid.Options.Digging"
        xmlns:cnv="clr-namespace:SteveCadwallader.CodeMaid.Converters">
    <DataTemplate DataType="{x:Type local:DiggingViewModel}">
        <StackPanel>
            <GroupBox Header="Navigation">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="When navigating center on" Margin="0,0,5,0" VerticalAlignment="Center" />
                    <RadioButton Content="whole item" IsChecked="{Binding CenterOnWhole}" Margin="5" />
                    <RadioButton Content="name only"
                            IsChecked="{Binding CenterOnWhole, Converter={x:Static cnv:BooleanInverseConverter.Default}}"
                            Margin="5" />
                </StackPanel>
            </GroupBox>
            <GroupBox Header="Display">
                <StackPanel>
                    <CheckBox Content="Show method parameters" IsChecked="{Binding ShowMethodParameters}" />

                    <CheckBox Content="Show item metadata" IsChecked="{Binding ShowItemMetadata}" />
                    <TextBlock FontStyle="Italic" Margin="25,0,0,0" Text="Example: 's' for static" />

                    <CheckBox Content="Show McCabe complexity" IsChecked="{Binding ShowItemComplexity}" />
                    <Grid IsEnabled="{Binding ShowItemComplexity}" Margin="25,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Warning threshold" Margin="0,0,5,0" VerticalAlignment="Center" />
                        <TextBox Grid.Column="1" Text="{Binding ComplexityWarningThreshold}" TextAlignment="Right"
                                Width="50" />

                        <TextBlock Grid.Row="1" Text="Alert threshold" Margin="0,0,5,0" VerticalAlignment="Center" />
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ComplexityAlertThreshold}"
                                TextAlignment="Right" Width="50" />
                    </Grid>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>